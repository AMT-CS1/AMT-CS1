program MaxThree
dictionary
    a, b, c, max_val : integer
algorithm
    read a
    read b
    read c
    if a >= b && a >= c then
        max_val <- a
    elif b >= c then
        max_val <- b
    else
        max_val <- c
    endif
    write max_val
endprogram
